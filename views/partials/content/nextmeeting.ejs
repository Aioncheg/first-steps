<article id="nextmeeting">
	<h1><i class="fa fa-calendar"></i>Next meeting</h1>
	<p>
		<img src="<%= appdata.meeting.where.img %>" alt="Meeting Location"> Our next meeting wil be on 
		<%= appdata.meeting.date %>, <%= appdata.meeting.time %> at 
		<%= appdata.meeting.where.name %> <%= appdata.meeting.where.address %>
	</p>
	<% include socialmedia.ejs %>
</article>
